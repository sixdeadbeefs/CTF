name: Update SixDeadBeefs Dockerhub Repo
on:
  push:
    paths:
      - 'docker/*'
jobs:
  Build-Docker-Image:
    - uses: actions/checkout@v2
    runs-on: ubuntu-latest
    steps:
      - run: cd $GITHUB_WORKSPACE/docker
      - run: docker build -t sixdeadbeefs/CTF:latest .
      - run: docker push sixdeadbeeds/CTF:latest      

